image: python:3.10

stages:
  - build
  - test
  - deploy

before_script:
  - python -m venv venv
  - source venv/bin/activate
  - pip install --upgrade pip

build:
  stage: build
  script:
    - echo "🔧 Building environment..."
    - python app.py

test:
  stage: test
  script:
    - echo "🧪 Running test..."
    - python -c "import app; app.main()"  # Simulates a test by calling main()

deploy:
  stage: deploy
  script:
    - echo "🚀 Deploying app.py..."
    - echo "Deployment complete!"  # Replace with actual deploy commands if needed
  only:
    - main
